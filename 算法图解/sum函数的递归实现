def sum_recursive(lst):
    """
    sum函数的递归实现
    :param lst:
    :return:
    """
    if len(lst) == 0:
        return 0
    return lst[0] + sum_recursive(lst[1:])


print(sum_recursive([1, 2, 3, 4, 5]))
